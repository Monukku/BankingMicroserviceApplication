apiVersion: security.istio.io/v1beta1
kind: PeerAuthentication
metadata:
  name: accounts-ms-mtls
  namespace: banking
spec:
  selector:
    matchLabels:
      app: accounts-ms
  mtls:
    mode: STRICT