apiVersion: v1
kind: Service
metadata:
  name: customers-service
  namespace: banking
  labels:
    app: customers-ms
spec:
  selector:
    app: customers-ms
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP