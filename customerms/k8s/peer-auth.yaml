apiVersion: security.istio.io/v1beta1
kind: PeerAuthentication
metadata:
  name: banking-mtls-strict
  namespace: banking
spec:
  mtls:
    mode: STRICT    # all traffic in banking namespace must be mTLS