apiVersion: security.istio.io/v1beta1
kind: PeerAuthentication
metadata:
  name: transactions-ms-mtls
  namespace: banking
spec:
  selector:
    matchLabels:
      app: transactions-ms
  mtls:
    mode: STRICT