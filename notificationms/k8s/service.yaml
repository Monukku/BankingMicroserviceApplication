apiVersion: v1
kind: Service
metadata:
  name: notifications-service
  namespace: banking
  labels:
    app: notifications-ms
spec:
  selector:
    app: notifications-ms
  ports:
    - name: http
      port: 9010
      targetPort: 9010
  type: ClusterIP