apiVersion: v1
kind: Service
metadata:
  name: cards-service
  namespace: banking
  labels:
    app: cards-ms
spec:
  selector:
    app: cards-ms
  ports:
    - name: http
      port: 8092
      targetPort: 8092
  type: ClusterIP