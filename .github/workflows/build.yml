name: SonarQube Multi-Service Analysis

on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  analyze:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          java-version: 17
          distribution: 'zulu'

      - name: Cache Maven packages
        uses: actions/cache@v4
        with:
          path: ~/.m2
          key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}
          restore-keys: ${{ runner.os }}-m2

<<<<<<< HEAD
          for service in "${services[@]}"; do
            echo "Building and analyzing $service"
            cd $service
            mvn -B verify org.sonarsource.scanner.maven:sonar-maven-plugin:sonar \
            -Dsonar.projectKey=monukku\
            -Dsonar.branch.name=main
            cd ..
          done
=======
      - name: Analyze Accounts Service
        run: mvn -f accounts/pom.xml clean verify sonar:sonar \
          -Dsonar.projectKey=rewabank_accounts \
          -Dsonar.projectName="Accounts Service" \
          -Dsonar.login=${{ secrets.SONAR_TOKEN }}

      - name: Analyze Loans Service
        run: mvn -f loans/pom.xml clean verify sonar:sonar \
          -Dsonar.projectKey=rewabank_loans \
          -Dsonar.projectName="Loans Service" \
          -Dsonar.login=${{ secrets.SONAR_TOKEN }}

      - name: Analyze Cards Service
        run: mvn -f cards/pom.xml clean verify sonar:sonar \
          -Dsonar.projectKey=rewabank_cards \
          -Dsonar.projectName="Cards Service" \
          -Dsonar.login=${{ secrets.SONAR_TOKEN }}
>>>>>>> f9f252f (chanes)
